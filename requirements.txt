# Core dependencies (no PyTorch!)
onnxruntime==1.17.0
tokenizers==0.15.0
hnswlib==0.8.0
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
numpy==1.24.0

# For model conversion (optional, only needed for setup)
# optimum[onnxruntime]==1.16.0
